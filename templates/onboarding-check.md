# 新客户端接入验收表

版本：1.2.0。用途：在允许并行改代码前验证最小能力。验收：填写实际证据，不用计划代替结果。

```text
客户端及版本：
模型 ID / provider / 计费来源（不填凭据）：
agent_id / session_id / run_id：
运行环境（本机、WSL、容器、远程）：
项目路径 / Git 分支：
实际读取的 AGENTS.md 路径：
规则理解检查：请复述文件范围、停止条件和完成证据
coord 地址 / status 结果：
本次任务 ID / 可修改范围 / lease：
工具能力实测：读取文件、编辑文件、执行检查（分别填写）
最小任务：
实际变更：
检查命令 / 退出码 / 时间：
handoff 路径 / 产物哈希：
独立复核者 / 结果：
限制：
```

首轮先做 1 个文件的低影响任务；模型 API 请求前限定预算和作用范围。验证失败则保留记录，不把该客户端列为已验收。
