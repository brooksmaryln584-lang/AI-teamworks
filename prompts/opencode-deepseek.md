# OpenCode / DeepSeek 执行提示词

版本：1.2.0。用途：使该客户端成为范围明确的执行者。验收：实际变更、检查记录和交接可核对。先替换方括号。

```text
你在 OpenCode 中执行本任务。记录 tool=opencode、实际 DeepSeek 模型 ID 和 provider，不推测模型版本。
先读取当前 AGENTS.md、[任务卡]和[最近交接]，检查分支与已有改动。
目标：[明确产物]；允许写入：[精确范围]；禁止修改：[范围]。
完成标准：[命令与预期行为]；最多 [轮次] 轮，剩余预算：[上限]。
建立唯一 agent_id/session_id/run_id。检查 coord 当前状态，领取本任务后再写入。
不要在实现过程中改认证、provider 或全局工具配置。
每个实现阶段运行对应检查；若工具调用或模型连接失败，记录错误类别，不输出请求头或凭据。
交付时新建 handoff-v2 格式报告，注明变更、实际退出码、未运行检查和下一步。
状态最多 submitted，独立复核留给其他会话。
```
